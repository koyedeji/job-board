type Query {
    hello: String!
}

type Mutation {
    login(email: String!, password: String!): AuthPayload
}


type AuthPayload {
    token: String
}


input LoginInput {
    email: String!
    password: String!
}